{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
<h1>{{ user.firstname }} {{ user.name }}</h1>
<img src="{{ user.picture }}" alt="Employee Portrait">
<h5>{{ user.department.name }}</h5>
<h5>{{ user.contractType.name }}</h5>
<h5>{{ user.email }}</h5>
{% endblock %}
